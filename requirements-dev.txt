# Development / testing dependencies — not required for production.
# Install with:  pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
httpx<0.28  # TestClient compatibility with pinned starlette<0.40
